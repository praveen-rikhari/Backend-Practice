<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🎦 Movie Talk</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<style>
    .card-img-top {
        width: 100%;
        height: 300px;
        object-fit: cover;
    }
</style>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">🎦 MovieTalk</a>
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                    <% if (!user) { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/register">Register</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/login">Login</a>
                        </li>
                        <% } else { %>
                            <li class="nav-item">
                                <a class="nav-link" href="/movies/create">Create</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/profile">Profile</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/logout">Logout</a>
                            </li>
                            <% } %>
                </ul>
            </div>
        </div>
    </nav>


    <div class="container mt-5">
        <h2 class="text-center">Welcome to MovieTlak 🎦.<br />Your favourite movie recommendation platform.</h2>
        <h3 class="text-left my-4 py-2 border-bottom border-danger border-3 d-inline-block">All recommended movies list
            🎥</h3>

        <% if (movies.length===0) { %>
            <p>No movies available yet.</p>
            <% } else { %>
                <div class="row">
                    <% movies.forEach(movie=> { %>
                        <div class="col-md-3 mb-4">
                            <div class="card h-100">
                                <img src="https://m.media-amazon.com/images/I/61RxB8tSkrL._AC_UF1000,1000_QL80_.jpg"
                                    class="card-img-top" alt="<%= movie.title %> Poster">

                                <div class="card-body">
                                    <h5 class="card-title">
                                        <%= movie.title %>
                                    </h5>
                                    <p class="card-text">
                                        <%= movie.description %>
                                    </p>

                                    <p><strong>IMDb ⭐:</strong>
                                        <%= movie.imdbRating %>/10
                                    </p>
                                    <span class="p-2 bg-warning rounded">
                                        <%= movie.genre%>
                                    </span>
                                    <p class="mt-3 text-muted">
                                        Posted by <span class="text-primary">@<%= movie.createdBy.username %></span>
                                    </p>
                                </div>
                            </div>
                        </div>
                        <% }); %>
                </div>
                <% } %>
    </div>


    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>